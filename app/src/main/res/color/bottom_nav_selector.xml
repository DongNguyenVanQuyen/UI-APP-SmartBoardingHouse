<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Active tab -->
    <item android:color="@color/purple_primary" android:state_checked="true" />

    <!-- Disabled state (optional but good practice) -->
    <item android:color="@color/text_hint" android:state_enabled="false" />

    <!-- Default -->
    <item android:color="@color/text_hint" />

</selector>